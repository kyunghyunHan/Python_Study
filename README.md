# Python_Study

## 설치

- brew로 pyenv설치

```
brew install pyenv
```

- python3설치

```
pyenv install --list
```

```
pyenv install 3.9.1
```

```
python --version
```

## 환경변수설정

- bash 사용시

```
vi ~/.bashrc
```

```
alias python="python3"
```

```
source ~/.bashrc
```

- zsh사용시

```
vi ~/.zshrc
```

```
alias python="python3"
```

```
source ~/.zshrc
```

```
python --version
```

- dkfkqkdhdadda
- 코드작성-컴파일러 -바이트코드-파이썬 가상머신-기계어

## 인터프리터와 컴파일러의 차이점

- 인터프리터와 컴파일러는 모두 프로그래밍 언어를 실행 가능한 기계어로 변환하는 역할을 한다.

- 하지만 이 두 가지 방식에는 몇 가지 차이점과 공통점이 있다.

- 먼저 공통점으로는 프로그램 코드를 실행 가능한 기계어로 변환하는 것이다.
- 이를 통해 프로그래밍 언어를 사용하여 작성된 코드를 실행할 수 있게 된다.
- 또한, 인터프리터와 컴파일러는 모두 프로그램 코드를 실행하기 전에 일종의 검사과정을 거쳐 문법 오류 등을 확인한다.

- 하지만 두 방식의 가장 큰 차이점은 프로그램 코드를 변환하는 시점이다.
- 컴파일러는 프로그램 코드를 실행하기 전에 먼저 전체 코드를 분석하고, 이를 바탕으로 실행 가능한 기계어로 번역한다.
- 이 과정에서 일반적으로 실행 파일이 생성되며, 이 파일을 실행하면 기계어로 번역된 코드가 실행된다.
- 이 방식은 실행 속도가 빠르다는 장점이 있다.

- 반면 인터프리터는 프로그램 코드를 한 줄씩 읽어들이면서 즉시 실행 가능한 기계어로 번역한다.
- 이 과정에서 실행 파일이 생성되지 않는다.
- 인터프리터는 실행 파일을 생성하지 않기 때문에, 프로그램 코드를 수정할 때마다 다시 번역하는 작업이 필요하지 않는다.
- 이 방식은 실행 속도가 느리다는 단점이 있다.

- 또한, 컴파일러는 대부분의 경우 언어의 특정 버전에 대해 지원이 되는 반면, 인터프리터는 일반적으로 다양한 버전의 언어를 지원한다.
- 이는 인터프리터가 코드를 실행할 때마다 번역을 수행하기 때문이다.

- 마지막으로, 인터프리터는 대화형 모드(interactive mode)를 지원하는 경우가 많다.
- 이는 사용자가 코드를 입력하면 즉시 실행 가능한 기계어로 번역하여 결과를 표시하는 모드이다.
- 이 방식은 테스트나 디버깅 작업 등에 유용하다.

- 따라서, 인터프리터와 컴파일러는 모두 프로그래밍 언어를 실행 가능한 기계어로 변환하는 공통점이 있지만, 프로그램 코드를 변환하는 시점이 다르며, 이에 따라 실행 속도나 언어 버전 지원 등에 차이가 있다.

## 인터프리터 종류

- 파이썬 (Python): 고수준 언어로 알려진 파이썬은 데이터 과학, 웹 개발, 인공지능, 자동화 등 다양한 분야에서 많이 사용

- 루아 (Lua): 게임 개발 및 임베디드 시스템에서 널리 사용되는 경량 스크립팅 언어로, 인터프리터 언어로 실행

- 자바스크립트 (JavaScript): 웹 브라우저에서 동적인 웹 페이지를 만들기 위해 사용되는 스크립팅 언어로, 인터프리터 언어로 동작

- 리스프 (Lisp): AI 연구 및 개발에서 널리 사용되는 함수형 프로그래밍 언어로, 인터프리터 언어로 동작

- 루비 (Ruby): 동적 객체지향 언어로, 인터프리터 언어로 실행되며, 웹 개발, 서버 사이드 스크립팅, 자동화 등 다양한 분야에서 사용

## 컴파일러 종류

- C (C): 절차적 프로그래밍 언어로, 시스템 프로그래밍, 임베디드 시스템, 시스템 드라이버 등의 분야에서 널리 사용

- C++ (C++): 객체지향 프로그래밍 언어로, C 언어를 확장하여 개발된 언어로, 시스템 프로그래밍, 게임 개발, 임베디드 시스템, 과학 및 엔지니어링 분야에서 널리 사용

- 자바 (Java): 객체지향 프로그래밍 언어로, 자바 가상 머신(JVM)에서 실행되는 플랫폼 독립적인 언어로, 웹 애플리케이션, 모바일 애플리케이션, 대규모 시스템 등에서 사용되는 널리 알려진 언어

- C# (C#): 마이크로소프트에서 개발한 객체지향 프로그래밍 언어로, .NET 프레임워크에서 실행되는 언어로, 윈도우 애플리케이션, 게임 개발, 웹 서비스 등에서 널리 사용

- 파스칼 (Pascal): 고수준 언어로 알려진 파스칼은 대학 및 교육용으로 많이 사용되었으며, 프로그래밍 기초 교육에서 많이 사용

##

현상 - 데이터 - 정보

- 연속적 데이터 ,범주적 데이터
- 진법변환
- 문자데이터= 인코딩
- ascii코드표
- 컴파일러 또는 인터프리터언어

## 하드웨어와 소프트웨어

- 하드웨어:물리적
- 소프트웨어:프로그램

## 소프트웨어

- 소프트웨어는 각각의 고유한 기능을 수행하는 하드웨어가 언제 어떻게 동작하여 문제를 해결하는지 지시하는 명령어 집합

- 응용소프트웨어 사용자의 업무나 목적에 맞게 문제 해결을 위한 처리 절차를 표현한 명령어 집합
- 시스템 소프트웨어 :하드웨어를 제어 관리하여 응용 소프트 웨어를 실행할수 있는 환경을 제공

## 하드웨어

- 컴퓨터가 처리할수 있는 2진수 형태로 변환

## 기억저장장치

- ROM
- RAM

## 특수 저장장치

- SSD

## 소프트웨어

## 운영체제의 역활

- 컴퓨터의 자원을 효율적으로 관리하고 응용 프로그램에 자원을 할당
- 사용자인터페이스
- 프로세스관리
- 네트워크 관리
- 기억장치관리
- 입출력장치관리

## 사용자 인터페이스

- CLI
- GUI

## 파이썬

- 다중프로그래밍 패러다임
